# Application Test Framework

## Building and Executing Tests 

- Navigate to `scu_proto/tests`
- Run `cmake -S . -B build` to create a build directory.
- Run `cmake --build build` to build the executable and execute all tests.

HTML Coverage Report will be generated in build/coverage
